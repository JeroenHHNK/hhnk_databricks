[project]
name = "my-project"
version = "0.1.0"
description = "Databricks-oriented groundwater timeseries utilities"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"
